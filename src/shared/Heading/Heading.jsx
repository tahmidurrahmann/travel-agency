const Heading = ({title}) => {
    return (
        <div>
            <h1 className="text-xl font-semibold uppercase text-center pt-8 lg:pt-16 md:text-3xl">{title}</h1>
        </div>
    );
};

export default Heading;